=========================
Open Loop Controller
=========================

This basic controller gives the opportunity to set the actuating variable :math:`u_{pwm}` manually.
It ignores the current state and output of the model,
as well as trajectory values.